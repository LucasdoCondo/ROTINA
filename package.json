{
  "name": "rotina",
  "version": "0.1.0",
  "private": true,
  "description": "ROTINA — SaaS multi-tenant. Monorepo: SPA (Vercel) + API (Vercel Functions / OCI) + PostgreSQL (Neon).",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "argon2": "^0.41.1",
    "pg": "^8.13.1"
  },
  "scripts": {
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "build:web": "npm run build --workspace frontend",
    "build:api": "npm run build --workspace backend",
    "dev:web": "npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace backend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend",
    "test": "npm run test --workspace backend && npm run test --workspace frontend",
    "smoke": "npm run smoke --workspace backend",
    "smoke:serverless": "npm run smoke:serverless --workspace backend",
    "db:deploy": "npm run db:deploy --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "db:seed": "npm run db:seed --workspace backend",
    "job:purge": "npm run job:purge --workspace backend",
    "worker": "npm run worker --workspace backend"
  }
}
